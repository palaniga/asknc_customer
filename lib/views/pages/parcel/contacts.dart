class Contact {
  String? fullName; // Contact's full name
  List<String>?
      phoneNumbers; // All phone numbers (iOS: all numbers, Android: selected number only)
  String?
      selectedPhoneNumber; // The specifically selected phone number when using selectPhoneNumber()
}
